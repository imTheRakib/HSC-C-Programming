#include<stdio.h>
#include<math.h>
int main()
{
    double num;

    scanf("%lf", &num);

    double result = sqrt(num);

    printf("Result = %lf\n", result);

    printf("Result = %lf", pow(num, (float)1/3));dou


}